import { motion, useReducedMotion } from 'framer-motion'
export default function DaySwitcher({ groups = [], selectedId, onSelect, todayGroupIds = [] }) {
  const reduceMotion=useReducedMotion()
  return <nav aria-label="Select day group" className="w-full"><ul className="-mx-4 flex snap-x gap-2 overflow-x-auto px-4 pb-1 pt-5 [scrollbar-width:none] sm:mx-0 sm:flex-wrap sm:px-0">{groups.map((group)=>{const active=group.id===selectedId; const today=todayGroupIds.includes(group.id); return <li key={group.id} className="relative flex-shrink-0">{today&&<span className="absolute -top-4 left-1/2 -translate-x-1/2 rounded-full bg-ink px-2 py-0.5 font-sans text-label font-bold uppercase text-white">Today</span>}<motion.button type="button" onClick={()=>onSelect(group.id)} aria-current={active?'page':undefined} whileTap={reduceMotion?undefined:{scale:.96}} className="min-h-touch rounded-full border-2 px-4 font-sans text-sm font-bold focus:outline-none focus-visible:ring-2 focus-visible:ring-offset-2" style={{borderColor:group.color,backgroundColor:active?group.color:`${group.color}18`,color:active?'#fff':group.color,['--tw-ring-color']:group.color}}>{group.name}</motion.button></li>})}</ul></nav>
}
