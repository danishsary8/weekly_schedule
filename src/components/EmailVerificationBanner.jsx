import { useState } from 'react'
import { MailCheck, Loader2 } from 'lucide-react'
import toast from 'react-hot-toast'
import { resendVerificationEmail } from '../api/services.js'
export default function EmailVerificationBanner(){const[loading,setLoading]=useState(false);const resend=async()=>{setLoading(true);try{const data=await resendVerificationEmail();toast.success(data.message)}catch(error){toast.error(error.message||'Could not resend the verification email.')}finally{setLoading(false)}};return <aside role="status" className="flex flex-col gap-3 rounded-2xl border-l-4 border-faith bg-paper p-4 shadow-card ring-1 ring-black/10 sm:flex-row sm:items-center"><span className="flex h-11 w-11 flex-shrink-0 items-center justify-center rounded-xl bg-faith/15 text-faith"><MailCheck className="h-5 w-5"/></span><div className="min-w-0 flex-1"><h2 className="font-sans text-sm font-bold text-ink">Verify your email to start building</h2><p className="mt-1 font-sans text-xs leading-relaxed text-ink/60">Check your inbox for the secure link. You can browse Daycraft now, but routine changes stay locked until verification.</p></div><button type="button" onClick={resend} disabled={loading} className="flex min-h-[44px] items-center justify-center gap-2 rounded-xl bg-ink px-4 font-sans text-sm font-bold text-white disabled:cursor-wait disabled:opacity-60">{loading&&<Loader2 className="h-4 w-4 animate-spin"/>}{loading?'Sending…':'Resend email'}</button></aside>}
