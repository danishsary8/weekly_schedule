# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr
*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
*.swp
.idea

# Env
.env
.env.*
!.env.example
!.env.production.example

# Local audit artifacts
# seed-audit-user.mjs writes a real Sanctum bearer token here, so it must never
# be committed. Regenerate it with:
#   node scripts/seed-audit-user.mjs > .audit-seed.json
.audit-seed.json
